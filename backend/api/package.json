{
  "name": "hlii-backend-api",
  "version": "0.1.0",
  "private": true,
  "description": "HLII backend API worker. Accepts ingest payloads from collector scripts and writes to D1 / R2.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.21.0"
  }
}
