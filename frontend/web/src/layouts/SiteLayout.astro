---
import "../styles/global.css";

interface Props {
  title: string;
  description?: string;
  activeNav?: "home" | "reports" | "search" | "method";
}

const {
  title,
  description = "Harbor Legal Independent Index tracks legislative sources across America and publishes data-driven reports on how legal activity is moving.",
  activeNav
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  </head>
  <body>
    <header class="site-header">
      <div class="container">
        <a class="brand" href="/">
          <img class="brand-mark" src="/logo-harbor.svg" alt="Harbor Legal Independent Index logo" width="40" height="40" />
          <span class="brand-kicker">HLII</span>
          <span class="brand-text">Harbor Legal Independent Index</span>
        </a>
        <nav class="nav-links">
          <a href="/" class:list={{ active: activeNav === "home" }}>Overview</a>
          <a href="/search" class:list={{ active: activeNav === "search" }}>Search</a>
          <a href="/reports" class:list={{ active: activeNav === "reports" }}>Reports</a>
          <a href="/method" class:list={{ active: activeNav === "method" }}>Method</a>
        </nav>
      </div>
    </header>

    <main>
      <slot />
    </main>

    <footer class="site-footer">
      <div class="container">
        <span>Harbor Legal Independent Index is a public data project. Reports are informational, not legal advice.</span>
        <span>MVP build - Reports published as PDFs</span>
      </div>
    </footer>
  </body>
</html>
