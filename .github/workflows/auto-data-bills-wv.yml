name: Auto Data · Bills · West Virginia

on:
  schedule:
    - cron: '33 3 * * *'
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - run: pip install -r backend/scripts/requirements.txt

      - name: Restore cursor
        uses: actions/cache@v4
        with:
          path: .cursor/wv.json
          key: cursor-wv-${{ github.run_number }}
          restore-keys: cursor-wv-

      - name: Run West Virginia collector
        run: python backend/scripts/bills/wv.py
        env:
          HLII_INGEST_TOKEN: ${{ secrets.HLII_INGEST_TOKEN }}

      - name: Save cursor
        uses: actions/cache@v4
        with:
          path: .cursor/wv.json
          key: cursor-wv-${{ github.run_number }}
