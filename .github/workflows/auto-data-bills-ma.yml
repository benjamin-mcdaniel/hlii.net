name: Auto Data · Bills · Massachusetts

on:
  schedule:
    - cron: '37 2 * * *'
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - run: pip install -r backend/scripts/requirements.txt

      - name: Restore cursor
        uses: actions/cache@v4
        with:
          path: .cursor/ma.json
          key: cursor-ma-${{ github.run_number }}
          restore-keys: cursor-ma-

      - name: Run Massachusetts collector
        run: python backend/scripts/bills/ma.py
        env:
          HLII_INGEST_TOKEN: ${{ secrets.HLII_INGEST_TOKEN }}

      - name: Save cursor
        uses: actions/cache@v4
        with:
          path: .cursor/ma.json
          key: cursor-ma-${{ github.run_number }}
