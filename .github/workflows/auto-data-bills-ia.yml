name: Auto Data · Bills · Iowa

on:
  schedule:
    - cron: '25 2 * * *'
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - run: pip install -r backend/scripts/requirements.txt

      - name: Restore cursor
        uses: actions/cache@v4
        with:
          path: .cursor/ia.json
          key: cursor-ia-${{ github.run_number }}
          restore-keys: cursor-ia-

      - name: Run Iowa collector
        run: python backend/scripts/bills/ia.py
        env:
          HLII_INGEST_TOKEN: ${{ secrets.HLII_INGEST_TOKEN }}

      - name: Save cursor
        uses: actions/cache@v4
        with:
          path: .cursor/ia.json
          key: cursor-ia-${{ github.run_number }}
