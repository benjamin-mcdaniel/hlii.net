name: Auto Data · Bills · South Dakota

on:
  schedule:
    - cron: '19 3 * * *'
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - run: pip install -r backend/scripts/requirements.txt

      - name: Restore cursor
        uses: actions/cache@v4
        with:
          path: .cursor/sd.json
          key: cursor-sd-${{ github.run_number }}
          restore-keys: cursor-sd-

      - name: Run South Dakota collector
        run: python backend/scripts/bills/sd.py
        env:
          HLII_INGEST_TOKEN: ${{ secrets.HLII_INGEST_TOKEN }}

      - name: Save cursor
        uses: actions/cache@v4
        with:
          path: .cursor/sd.json
          key: cursor-sd-${{ github.run_number }}
